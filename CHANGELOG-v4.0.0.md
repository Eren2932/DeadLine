# DeadLine v4.0.0 — «Пояс глубины»

Версия в `project.godot`: `3.1.0` → `4.0.0`. Линт и проверка ассетов: **0 ошибок, 0 предупреждений**.

---

## 1. Новое пространство (вариант A)

Раньше глубины не существовало: были два `clampf` по экранному `Y`
(`BAND_TOP = -44`, `BAND_BOT = 34`). Верхняя граница визуально попадала на забор —
а забор живёт на слое параллакса 0.62, то есть в другом пространстве, отсюда и
«персонаж лезет на забор». Нижняя упиралась в осевую разметку, и весь асфальт
ниже неё был мёртвым — отсюда «невидимая стена под жёлтой линией».

Теперь пространство описано в одном файле — **`scripts/World.gd`** (автолоад `World`):

```
depth 0.0 -> дальний бордюр, под основанием забора   (мельче, рисуется раньше)
depth 1.0 -> ближний бордюр, брошенные машины        (крупнее, перекрывает)
screen_y  =  BELT_TOP + depth * BELT_H               (производная величина)
```

| Параметр | Значение | Что даёт |
|---|---|---|
| `HORIZON_Y` | −34 | основание забора, дальний край мира |
| `CURB_H` | 12 | дальний тротуар, только декор |
| `BELT_TOP` / `BELT_BOT` | −22 / 72 | границы **нарисованы**, а не невидимы |
| `BELT_H` | 94 | было 78, из них половина за стеной |
| `SCALE_FAR` / `SCALE_NEAR` | 0.88 / 1.10 | перспектива видна телом и тенью |
| `DEPTH_SPEED` | 0.62 | шаг вглубь не выглядит вдвое быстрее шага вбок |
| `CAM_DEADZONE` | 28 | камера не плавает под ногами |

`ENABLED = false` возвращает геометрию v3 целиком — одна строка на откат.

## 2. Что переехало на пояс

- **Player** — скорость по глубине через `World.move_vec`, масштаб и `z_index` от
  `depth`, тень масштабируется вместе с телом (именно она приземляет спрайт).
- **Zombie** — вход на случайной глубине, личное смещение коридора (`lane_bias`),
  подход по диагонали. Толпа больше не марширует одной шеренгой.
- **Bullet** — попадание по **коридору глубины** (`corridor_hit`), а не по
  пиксельному `Y`. Появилось поле `corridor`: у дробовика коридор шире, у
  снайперки уже — стволы наконец различаются не только цифрой урона.
- **Гранаты** — убрана подгонка `r + 44.0`, которая делала взрыв горизонтальной
  строкой. `Y` уже в мировых пикселях, взрыв бьёт площадью.
- **Ally / BuildGhost / стройка / спавны** — все клампы через `World`.
- **Сортировка** — `y_sort_enabled` выключен, порядок даёт `World.z_at(depth)`.
  Они спорили друг с другом и перекладывали спрайты поверх дорожных слоёв.
- **Миграция сейвов** — в снапшот пишется `space: 4`. Старый сейв (`space: 0`)
  пропорционально перекладывается на новый пояс: никто не телепортируется в забор.

## 3. Окружение: 11 слоёв, 4 темы

`tools/env.py` (перенесён из HTML в проект) генерирует все темы одной командой.
Геометрия впечатана в бейки: строка 206 картинки = `World.BELT_TOP`, поэтому
слои кладутся одним смещением `BELT_ART_Y` и не подгоняются руками.

Слои и параллакс: `sky 0.00 · moon 0.04 · far 0.20 · mid 0.40 · near 0.66 ·
fence 0.80 · fog 0.86 · curb_far / road / curb_near 1.00 · props_near 1.00 (z=120)`.

Параллакс привязан к **камере**, а не ко времени — иначе город едет, когда игрок стоит.

Правки после первого прогона: забор стал просвечивающей сеткой, асфальт поднят на
2 тона (кровь и зомби не сливаются), с луны убраны полосы шейдинга, чёрная зона
внизу закрыта ближним бордюром. Машины получили **4 архетипа кузова** (седан, фургон,
пикап, разбитая), разброс тона и рваный шаг — ритм «через равные промежутки» и
читался как обои. Все 4 темы: **276 КБ**.

Темы: `NEON DECAY`, `BLOOD MOON`, `TOXIC HAZE`, `DEAD COLD` — выбор в настройках,
пятая добавляется папкой, без правок кода.

## 4. Управление под большой палец

Было 8 кнопок фиксированными координатами, часть — вне зоны досягаемости,
радиусы 12–16 px (меньше физического минимума 9 мм).

- Кластер строится **процедурно**: угловой шаг считается из хорды
  `(r1 + r2 + MIN_GAP)`, поэтому зазор — следствие математики, а не расстановки
  координат. Радиус дуги подбирается так, чтобы всё влезло; при 6+ кнопках
  лишние уходят на внутреннее кольцо.
- `MIN_R = 15` px (~9 мм), `MIN_GAP = 10` px.
- **Кнопки контекстные**: в бою 3–4. `RLD` — только когда магазин неполон,
  `BLD` — только в подготовке, `TYP` — веер типов гранат по долгому нажатию
  (короткий тап бросает, удержание выбирает и броска не тратит).
- Новые настройки: `hud_scale` (compact/normal/large), `hud_alpha`, `env_theme`.

## 5. Линт, который это держит

`tools/lint.py` пополнен правилами ровно на те грабли, на которые мы уже наступали:

1. `GameData.BAND_*` обязан совпадать с `World.BELT_*` — два источника чисел
   больше не могут разъехаться.
2. Никто, кроме `World.gd`, не пишет свой `clampf` по `position.y`.
3. `y_sort_enabled = true` запрещён.
4. `BELT_H == BELT_BOT - BELT_TOP`, `SCALE_NEAR > SCALE_FAR`, `DEPTH_SPEED` в
   диапазоне 0.40–0.85.
5. Раскладка кнопок **симулируется тем же алгоритмом** на 6 разрешениях × 3
   размерах HUD × 2 сторонах джойстика × 5 наборах кнопок: перекрытие или выезд
   за экран = ошибка сборки.
6. Каждая тема обязана иметь все 11 слоёв.

Правило №5 сразу отработало: поймало **баг в первой версии моей же дуги** —
на 640×300 и 778×360 обрезка по краям экрана сталкивала кнопки в кашу, а первая
кнопка на 8° резалась о нижнюю кромку. Обе причины устранены: радиус подбирается
до раскладки, окно дуги 20°–88°, просвет считается из радиуса кнопки.

---

## Что осталось (честно)

- Машины в `props_near` подрезаны срезом экрана и читаются суховато — им нужен
  свет от фар и битые стёкла, это чистая косметика бейка.
- Тени зомби пока эллипс от `smoke`; под крупного босса стоит сделать свою.
- Веер гранат не проверяется линтом на перекрытие с кластером (он модальный,
  рисуется поверх), но правило добавить стоит.
